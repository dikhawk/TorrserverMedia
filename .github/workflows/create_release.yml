name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Save upload_url to artifact
        uses: actions/upload-artifact@v3
        with:
          name: release-info
          path: upload_url.txt
          retention-days: 1 # Опционально: как долго хранить artifact
        env:
          UPLOAD_URL: ${{ steps.create_release.outputs.upload_url }}

#      - name: Create Release
#        id: create_release
#        uses: actions/create-release@v1
#        env:
#          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
#        with:
#          tag_name: ${{ github.ref }}
#          release_name: Release ${{ github.ref }}
#          body: |
#            Release TorServerMedia.
#
#      - name: Set Release ID as output
#        id: set_release_id
#        run: echo "::set-output name=release_id::${{ steps.create_release.outputs.id }}"
#    outputs:
#      release_id: ${{ steps.set_release_id.outputs.release_id }}